# Copy this file to .env and set at least one provider key.
# Used by docker compose local development.

# Optional: set a stable compose project/volume prefix.
COMPOSE_PROJECT_NAME=clawdly-deploy

# Optional: explicit network for per-user gateway containers.
# OPENCLAW_DEPLOY_DOCKER_NETWORK=${COMPOSE_PROJECT_NAME}_default

# Optional: override gateway runtime UID/GID if using a custom image user.
# OPENCLAW_DEPLOY_DOCKER_GATEWAY_UID=1000
# OPENCLAW_DEPLOY_DOCKER_GATEWAY_GID=1000

# Optional: protect /admin and /v1/admin/* with a token.
# OPENCLAW_DEPLOY_ADMIN_TOKEN=change-me

# URL that gateway containers use to call back to this deploy server for
# profile enrichment. Must be reachable from inside the Docker network.
# Default works with the included docker-compose.yml (service name "deploy").
# OPENCLAW_DEPLOY_INTERNAL_URL=http://deploy:8080

# Provider API keys (set at least one).
ANTHROPIC_API_KEY=
CEREBRAS_API_KEY=
GEMINI_API_KEY=
GOOGLE_API_KEY=
GROQ_API_KEY=
MISTRAL_API_KEY=
NVIDIA_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
PERPLEXITY_API_KEY=
TOGETHER_API_KEY=
XAI_API_KEY=
